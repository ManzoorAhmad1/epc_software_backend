services:
  - type: web
    name: epc-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGO_URI
        sync: false
      - key: MAPBOX_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
